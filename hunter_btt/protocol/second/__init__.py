"""Second-generation Hunter BTT protocol."""

# The existing protocol implementation remains in protocol.packets,
# protocol.parser, and protocol.notifications during the migration.
# Moving those modules is a later, behavior-preserving step.
